input {
  file {
    path => "/home/shatakshi/Downloads/End-to-end-Object-Detection-Project-main/log/*.log"
    start_position => "beginning"
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "application-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
